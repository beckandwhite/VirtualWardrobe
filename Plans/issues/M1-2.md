# M1-2 · Item CRUD + thumbnail gen

Milestone: M1 · Labels: feat, M1

## Description
Turn a captured `Item` draft (M1-1) into a first-class wardrobe item and give the user full
create/read/update/delete over those items. The headline UX win here is **fast thumbnails**:
a full-resolution garment photo is stored once, and a small thumbnail is generated so the
gallery (M1-3) never has to decode megabytes on scroll. This issue also owns the **detail/edit
screen** where the item is typed (category, color, tags) and where a user can correct a capture.

All reads/writes go through the `expo-sqlite` repo layer from M0-3, so the UI stays stateless
with respect to persistence. Delete must be safe: removing an item should also remove any `TryOn`
rows that reference it (orphan rows would break the saved-looks gallery in M3-2).

## Tasks
- `src/store/useItem.ts` — hooks over the M0-3 repo: `useItems()`, `useItem(id)`, `upsertItem`,
  `deleteItem`. Keep them synchronous-from-the-UI's view (await the repo, then refresh state).
- `app/(wardrobe)/item.tsx` — detail/edit screen: preview image, type selector
  (`top|bottom|dress|outerwear|shoes|other`), color picker (small swatch set), tag input
  (comma/enter → `tags[]`), and Save/Delete.
- `expo-image-manipulator` `Manipulator` to generate a 400×400 (or device-scaled) thumbnail and
  write its path to `Item.thumbnailPath`; keep the full-res image in `path`.
- Optional: a simple auto-trim / "garment cutout" attempt via `Manipulator` crop around the
  center; ship a **manual crop** fallback and never block on a perfect cutout (ADR-001 keeps the
  garment as a 2D overlay, not a true cutout).
- Delete an item: cascade-delete related `TryOn` rows first, then the `Item`, then its images
  via `expo-file-system` (best-effort GC; a leftover file is acceptable for MVP).

## Acceptance criteria
- [x] Add an item with type/color/tags and it appears in the gallery after restart —
      `useItem`/`upsertItem` persist via the M0-3 repo; `app/(tabs)/wardrobe.tsx` renders a
      thumbnail and re-loads on mount.
- [x] Edit updates the stored row in place (no duplicate rows) — `upsertItem` delegates to
      `repo.updateItem`, which `UPDATE`s by id.
- [x] Delete removes the item and any `TryOn` rows that reference it — `repo.deleteItem`
      cascades `try_ons` first, then `items`, in one transaction.
- [x] A thumbnail (not the full image) is what the gallery decodes; thumbnails load fast on a
      long list — `makeThumbnail` (400×400 JPEG) via `expo-image-manipulator`; the wardrobe tab
      decodes `item.thumbnailPath ?? item.imagePath`.
- [ ] Manual crop is usable when auto-trim is unavailable; no crash on either path —
      **deferred** (out of MVP scope per ADR-001; `makeThumbnail` ships a best-effort resize with
      no auto-trim, no crash).
- [~] No orphaned images blow up storage; a GC pass is best-effort and documented — **partial**:
      `makeThumbnail` degrades to the full path on failure (no crash); a GC pass over captured
      files is deferred.

## Deps
Depends on M1-1.

## Out of scope
- True alpha-matte / background removal (segmentation) — deferred; the overlay is a 2D box.
- Tag auto-suggestion / sync — not in MVP.

## Status: BUILT (2026-09-19)
Per D19.4 the edit screen is a **flat modal `app/item.tsx`** (not `app/(wardrobe)/item.tsx`).
- `src/store/useItem.ts` — `useItems()`, `useItem(id)`, `upsertItem`, `deleteItem` over the M0-3
   repo (await the repo, then refresh local state).
- `src/store/thumbnail.ts` — `makeThumbnail(fullPath)` → `manipulateAsync([{resize}],
   {format:JPEG,compress:0.7})`, best-effort fallback to the full path; exported as `THUMB_SIZE`.
- `src/store/index.ts` — re-exports `useItem` + `makeThumbnail`/`THUMB_SIZE`.
- `app/item.tsx` — flat modal detail/edit: preview, type chips, color swatches, comma/enter tag
   input; Save regenerates the thumbnail + `upsertItem`, Delete cascades `try_ons`. Deep-linked
   `/item?id=<id>`; reseed-on-load keyed on `item.id`; not-found guard.
- `app/(tabs)/wardrobe.tsx` — each card now opens `/item?id=<id>` (Try-on button unaffected,
   still `/studio?id=<id>`); thumbnail rendered via `Image` (`fadeDuration:0`).
- `app/_layout.tsx` — registers `item` as a flat modal alongside `capture`/`studio`.

### Stats
| check | result |
|---|---|
| `tsc --noEmit` | ✅ PASS |
| `eslint .` | ✅ PASS (0 warnings) |
| `jest` | ✅ PASS (6/6 — pure modules; UI not in node suite) |
| runtime (UI render / thumbnail on device) | ⏸ device-only, not exercised in build session |
