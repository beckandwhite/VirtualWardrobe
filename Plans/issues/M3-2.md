# M3-2 · Saved-looks gallery + export/share sheet

Milestone: M3 · Labels: feat, ux, M3

## Description
Promote the studio's minimal saved-looks list (M2-3) into a first-class gallery.

## Tasks
- A "Looks" screen listing all `TryOn` rows with a preview thumbnail.
- Tap → reopen / re-share / delete.
- Unified export/share sheet across studio and gallery.

## Acceptance criteria
- [ ] All saved looks appear, newest first.
- [ ] Delete a look removes its row (and keeps the wardrobe item).
- [ ] Share works from both studio and gallery.

## Deps
Depends on M2-3.
