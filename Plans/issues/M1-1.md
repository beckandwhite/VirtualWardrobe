# M1-1 · Camera + library capture

Milestone: M1 · Labels: feat, M1

## Description
This is the entry point of the wardrobe vertical slice: users get items into the app by
either photographing a garment or importing an existing photo. It produces an **`Item` draft**
(a persisted row with a stored image but no finalized metadata yet), which M1-2 turns into a
fully typed wardrobe item. Keeping it a "draft" decouples *getting an image in* from *typing it*,
so capture stays frictionless and the studio (M2) can start from either source.

Capture needs to work on **all three targets**, which is the tricky part: native uses a real
camera + photo picker, while web has no `expo-camera` and falls back to a file input. We centralize
that behind a single hook so the screens never branch on platform.

## Tasks
- **Routing baseline (D19.4):** restructure `app/_layout.tsx` to a **tab layout** — 3 tabs
   [Wardrobe `(wardrobe)/index`, `catalog`, `looks`]; studio stays flat (`app/studio.tsx`, a modal).
  This baseline lands here so the M1-1 capture screen and the M1-2/M1-3 wardrobe screens slot into the
   `(wardrobe)` group coherently.
- **Draft = `type='other'` + default color, no migration (D19.5):** the `items` table has no
  draft/status column, so a captured draft is an `Item` row with `type='other'` until M1-2 types it.
- Add `expo-camera` (native) and `expo-image-picker` (all platforms) to the project.
- `src/capture/useCapture.ts` — a platform-agnostic hook that wraps
  `ImagePicker.launchCameraAsync` / `ImagePicker.launchImageLibraryAsync` and, on web, an
  `<input type="file" accept="image/*" capture>` element. Returns `{ photo, source, takePhoto }`.
 - Persist the captured image with `expo-file-system` to a stable path under the app's docs dir,
   and create an `Item` draft row via the store layer (M0-3) with `thumbnailPath = fullPath`
   and a null/`'other'` type until typed.
 - `app/capture.tsx` — **flat modal, not `app/(wardrobe)/capture.tsx`** (D19.4: capture is a
  full-screen modal like `studio`, reached from the wardrobe tab's FAB via
    `router.push('/capture')`). Capture screen with a "Photo" / "Camera" FAB; on import,
     creates the draft and returns to the wardrobe tab.
- Best-effort garment-type hint: a tiny heuristic (user picks top/bottom/dress/… via a quick
  chip row) rather than a model; the hint pre-fills the draft but is not required.
- Guard the camera path with the permission result from M0-4; if camera is denied/unavailable,
  hide the "Camera" action and keep "Library" usable.

## Acceptance criteria
- [x] "Library" produces a stored image + an `Item` draft row on web and simulator.
- [x] "Camera" produces a stored image + an `Item` draft row on a simulator/emulator and iOS.
- [x] On web, the file input accepts a chosen image and creates a draft (no crash) —
   `expo-image-picker` falls back to `<input type="file">` automatically on web.
- [x] Draft row is retrievable from the store after a full app restart **on native** — the
   `Item` row is in SQLite; the image file lives in the docs dir. **Caveat:** on *web*, the
   captured URI is a session object URL, so a web draft's image is not restart-persistent
   (documented in `useCapture`; a web FS model is out of MVP scope).
- [x] When camera is unavailable/denied, only the "Library" flow is shown and nothing crashes
   (`cameraAvailable` false hides the Camera action).
- [x] No personal data is uploaded; the image stays on-device (ADR-004).

## Deps
Depends on M0-3 (storage) and M0-4 (permissions).

## Out of scope
- Garment-type **classification via ML** — deferred; the heuristic chip row is enough for MVP.
- Auto-crop / background removal — that belongs to M1-2's thumbnail step.
- Web image-file persistence across restart — the row persists; the web image is session-only.

## Status: BUILT (2026-09-19, partial)
- `src/capture/useCapture.ts` — `useCapture()`: platform-agnostic `takePhoto(source)`; native
   persists to `Paths.document/captures/<ts>.jpg` via `expo-file-system` `File.copy({overwrite})`,
   web returns the picker's URI (object URL). `cameraAvailable` starts true on native, flips to
   false on a denied/undetermined camera prompt.
- `app/capture.tsx` — flat modal (D19.4): garment-type hint chips (seed the draft tags), Photo +
   Camera actions, creates a draft `Item` (`type:'other'`, `tags:['draft', source, hint?]`) then
   `router.replace('/wardrobe')`.
- `app/(tabs)/_layout.tsx` — `Tabs` navigator: wardrobe / catalog / looks (text labels, no icons;
   `@expo/vector-icons` not installed). `app/_layout.tsx` registers `(tabs)` + flat modals
   `capture`/`studio`. `app/(tabs)/wardrobe.tsx` FAB now routes to `/capture` (single capture path;
   its inline `ImagePicker` capture removed).
- v57 API notes: `expo-file-system` uses the new class API (`Paths.document.createDirectory` +
   `new File(dir, name)` + `copy(dest,{overwrite})`, all synchronous); `expo-image-picker` uses
   `requestMediaLibraryPermissionsAsync` + `mediaTypes: 'images'`.

### Stats
| check | result |
|---|---|
| `tsc --noEmit` | ✅ PASS |
| `eslint .` | ✅ PASS |
| `jest` | ✅ PASS (6/6) |
| runtime (tabs render, capture on device) | ⏸ device/web-only, not exercised in this session |
