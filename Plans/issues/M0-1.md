# M0-1 · Initialize Expo + TS strict + Expo Router + lint/format

Milestone: M0 · Labels: feat, M0

## Description
Stand up the Expo project skeleton so the rest of the plan has a home to build on.

## Tasks
- Run `npx create-expo-app` with the **TypeScript** template, inside `VirtualWardrobe/`.
   (Use `nvm use 22` first — Node 26 is active; see ADR-010.)
- Add Expo Router file-based routing under `app/`.
- Add ESLint + Prettier; wire to the project.
- Create the folder structure from `Plans/01-architecture.md §1` (`app/(onboarding)`,
   `app/(wardrobe)`, `app/studio`, `app/catalog`, `src/store`, `src/pose`,
   `src/composer`, `tests`, `assets`).
- Add a `tsconfig` with `strict: true`.

## Acceptance criteria
- [ ] `npx expo start` boots on web **and** a native simulator.
- [ ] Expo Router renders the `(onboarding)` group by default.
- [ ] `tsc --noEmit` clean; `eslint .` clean.
- [ ] Folder structure exists and is reflected in the repo.

## Notes
Defer any feature code. This is the scaffolding only.
