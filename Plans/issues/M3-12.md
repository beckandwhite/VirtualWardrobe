# M3-12 · [autonomous] Docs: how to stand up the iOS test environment

Milestone: M3 · Labels: docs, tooling, qa, M3

## Autonomous-subagent brief (work this alone, no human in the loop)
You are executing this work item **fully autonomously**. No human is available for mid-task
decisions. Operate to the acceptance criteria, record every material call in the **Decision
Log** at the bottom (mirrored into `Plans/decision-log.md`), and finish in one of three
terminal states: **DONE / NO-GO / PARTIAL**. Never stall for input — if blocked, *default* to the
lowest-risk option, note why, and continue. Do **not** commit or open a PR; leave the tree clean.

## Problem
`docs/dev-setup.md` §7.2 states the dev env **can't run the iOS simulator**, and there are no
instructions for standing one up — yet acceptance criteria assume it: M1-1 AC1 reads *"Camera"
produces a stored image + Item draft row on a simulator/emulator and iOS.* Without a documented
iOS test env, on-device/camera behavior on iOS is unverifiable and device-touching ACs can only be
"waived." This item writes that doc.

This is the **documentation half** of the iOS pair; **M3-13** performs the *actual* setup on this
MacBook. They share a target; this issue produces prose + commands, M3-13 executes them.

## Goal
Add a precise, copy-pasteable section to `docs/dev-setup.md` (new **§10 "iOS test environment"**
or extend §3/§7) that tells any dev how to build + launch the app on an **iOS Simulator (or a
device)** and run the device-touching tests — so M1-1's camera/Item-draft AC and native smoke can
actually be exercised.

## Context (what already exists — reference, don't duplicate)
- Platform split: `@tensorflow*` is excluded from the native bundle (dev-setup §3); native uses
  the **manual** `PoseProvider` (`providers.native.ts` / M2-4). Document that the iOS test env
  verifies the manual path (not MoveNet).
- On-device ML (MediaPipe) is a separate NO-GO spike: `Plans/issues/M3-1.md` +
   `Plans/spikes/M3-1-mediapipe-native-pose.md` — do **not** re-scope this item into that.
- EAS prebuild is the known path for native custom code (Expo Go can't run it); reference M3-1
  rather than re-deriving it. This machine **is** a mac, so the iOS simulator is the natural target.
- Current stated limitation: `docs/dev-setup.md` §7.2.

## Tasks
- In `docs/dev-setup.md`, add an iOS test-env section covering:
   1. **Toolchain:** Xcode (version + a recent iOS Simulator runtime), command-line tools,
    `xcrun simctl` for device management.
   2. **Launch:** `npx expo start --ios` (Expo Go on the Simulator) for the baseline; when native
      custom code is needed, an **EAS prebuilt/preview** client (`eas build --profile preview`)
       — reference M3-1, don't reproduce it.
   3. **Simulator choice:** which runtime (e.g. an iPhone model + iOS version), `simctl` list/boot/
      open, GPU/rendering, and the headless-vs-visible note for any automated run.
   4. **Test surface:** how to drive M1-1's device-touching AC (camera → Item-draft row) — manual
       smoke today; note a future XCUITest / E2E layer (out of scope here).
   5. **Gotchas:** Xcode + runtime download size, signing on a real device, first-build slowness,
      and the sandbox ceiling (see Notes).
- **Do not** boot the Simulator in this issue — that's M3-13. Prose + commands only.
- Cross-link from §7.2 to the new section.

## Acceptance criteria
- [ ] `docs/dev-setup.md` has an iOS test-env section: Xcode + simulator runtime + `simctl` +
        launch (Expo Go vs EAS prebuild) + how to exercise M1-1's camera/Item-draft AC.
- [ ] The manual-native path (not MoveNet) is stated; MediaPipe/ML re-scope is explicitly out.
- [ ] §7.2 points to the new section; `docs/` stays internally consistent.
- [ ] Decision Log (below) records the commands chosen + any Xcode/runtime nuance.

## Decision log (autonomous — newest at top)
- _empty until run; mirror headline decisions into `Plans/decision-log.md`._

## Notes
- This is the durable answer to §7.2 for iOS; pair it with M3-13 (which proves it on this mac).
  Because this host **is** macOS, iOS is the platform most likely to actually pass here.
- The on-device *pose* capability is M3-1's domain and is NO-GO in-sandbox; this item is about the
  **test environment**, not on-device ML.

## Deps
- Enables M1-1 AC1 (camera/Item-draft on a real iOS target). Related: M3-1 (prebuild ref), M3-3
  release build. Pairs with M3-13 (actual setup). Independent of Playwright (M3-8/9) + Android (M3-10/11).

## Terminal state
[ ] DONE &nbsp; [ ] NO-GO &nbsp; [ ] PARTIAL — _fill in on completion._

## Out of scope
- Executing the simulator setup (M3-13).
- On-device ML / MediaPipe pose (M3-1). XCUITest/E2E suites (future).
